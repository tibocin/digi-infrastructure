# =============================================================================
# DIGI INFRASTRUCTURE ENVIRONMENT VARIABLES
# =============================================================================
# 
# CRITICAL: This file contains NO actual secrets - only examples!
# Copy this file to .env and fill in your actual values
# NEVER commit .env files to version control
#
# =============================================================================

# PostgreSQL Configuration
POSTGRES_USER=digi
POSTGRES_PASSWORD=your_secure_postgres_password_here
POSTGRES_DB=digi
POSTGRES_MULTIPLE_DATABASES=digi_core,lernmi,beep_boop,pcs

# PCS Database Credentials
PCS_DB_PASSWORD=your_secure_pcs_db_password_here

# Neo4j Configuration
NEO4J_PASSWORD=your_secure_neo4j_password_here

# PCS Security Keys (CRITICAL - Generate unique values!)
PCS_SECURITY_SECRET_KEY=your_64_character_secret_key_here
PCS_SECURITY_JWT_SECRET_KEY=your_64_character_jwt_secret_here

# Grafana Configuration
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=your_secure_grafana_password_here

# pgAdmin Configuration (Development)
PGADMIN_DEFAULT_PASSWORD=your_secure_pgadmin_password_here

# AWS Backup Configuration (Optional)
AWS_ACCESS_KEY_ID=your_aws_access_key_here
AWS_SECRET_ACCESS_KEY=your_aws_secret_key_here
RESTIC_REPOSITORY=your_restic_repository_here
RESTIC_PASSWORD=your_secure_restic_password_here

# =============================================================================
# SECURITY REQUIREMENTS
# =============================================================================
#
# 1. All passwords must be at least 16 characters
# 2. Use a mix of uppercase, lowercase, numbers, and special characters
# 3. Generate unique values for each environment (dev, staging, prod)
# 4. Rotate credentials regularly (every 90 days recommended)
# 5. Use a password manager or secrets management system
#
# =============================================================================
# GENERATION COMMANDS
# =============================================================================
#
# Generate secure passwords:
# openssl rand -base64 32
# 
# Generate secure keys:
# openssl rand -hex 32
#
# =============================================================================
